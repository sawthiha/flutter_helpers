name: hive_helpers
description: Hive Helpers
version: 0.0.1
homepage:

environment:
  sdk: '>=3.2.0-22.0.dev <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.0.6
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
